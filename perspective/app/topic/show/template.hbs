<h1>{{model.name}}</h1>
<section class="row">
    {{#each model.articles as |article|}}
        <div class="article-div col-md-4 col-xs-12">
            <div class="box article">
                {{article.name}}
            </div>
        </div>
    {{/each}}
</section>
